# specz - specs tracebility AI framework

specz is a traceability layer that wraps around whichever dev framework does the thinking (superpowers, gstack, or plain chat). It owns spec format, mapping, and consistency — never brainstorming, planning, or implementation themselves.

Here's the proposed workflow with skills hanging off it.
